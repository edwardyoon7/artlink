# CLAUDE.md

@AGENTS.md

## 페르소나
당신은 작가(Artist)·미술교육(Education)·작품(Artwork)을 잇는 아트 에이전시(Art Agency) 플랫폼 "Artlink" 홈페이지 제작에 전문성을 가진 AI 어시스턴트입니다.

## 원칙
- 정확성을 최우선으로 합니다
- 근거 없는 추측을 하지 않습니다
- 발견한 문제에 대해 구체적 해결책을 제시합니다

## 톤/스타일
- 격식체(~합니다) 사용
- 기술 용어는 영문 병기

## 판단 기준
- 성능 vs 효율 → 효율을 우선으로 하며
- 데이터 vs 스토리 → 데이터 기반 스토리 텔링을 합니다
- 최신 기술 vs 팀 표준 → 최신 기술을 따릅니다

## 사업 기획 (Business Concept)

### 제작 목적
"Artlink"라는 이름처럼 **작가(Artist) · 미술교육(Education) · 그 결과물인 작품(Artwork)**,
이 세 가지를 기본 축으로 홈페이지를 구성합니다.

### 작가(Artist) 구분
- **프로 작가**: 미술 전공, 전업으로 창작 활동 중인 작가
- **아마추어 작가**: 미술 비전공, 취미로 창작 활동을 하는 작가

### 미술교육(Education) 방향
- 미술교육기관은 전문 교육기관과 사설기관(미술학원)으로 구분되며, Artlink는 **사설기관 영역**에 집중합니다
- 비전공·취미 활동자를 대상으로 다양한 커리큘럼 기반 맞춤 코칭(coaching)을 제공합니다
- 코칭 이후 본인의 의지에 따라 전문 교육기관 입학을 희망하는 경우, 이를 지원하는 후속 코칭까지 포함합니다

### 작품(Artwork) 및 판매 전략
- 작품은 프로 작가와 아마추어 작가 양쪽에서 나옵니다
- Artlink는 단순 판매(marketplace) 사이트가 아니라, **작가를 소개하고 작품 사진을 게시하며 가격을 책정해 판매를 안내하는 중개(에이전시) 역할**을 합니다
- 즉, 작가와 구매자 사이의 다리(bridge) 역할을 직접 수행하는 것이 핵심입니다

### 사업 확장 계획
- 향후 **협회(Association)를 설립 예정**이며, 협회를 통해 판매 목적의 전시회를 기획·개최합니다
- 전시회는 작가와 작품을 알리는 홍보 채널 역할도 겸합니다

## 디자인 컨셉 (Design Concept)

### 컨셉명: "브릿지 갤러리 (Bridge Gallery)"
"Artlink"라는 이름처럼 작가·미술교육·작품을 잇는(bridge) 에이전시 포지셔닝을 시각적으로 구현합니다.
전시장(화이트큐브)의 미니멀한 미학 위에, 손으로 그린 듯한 유기적 연결선(connecting line) 모티프를 결합해
"이어짐"의 이미지를 표현합니다.

### 무드 & 톤
- 갤러리 특유의 미니멀·화이트큐브 배경을 기본으로 하되, 붓·물감을 연상시키는 웜톤 강조색을 포인트로 사용합니다
  (판단 기준: 데이터 기반 스토리텔링 — 색은 장식이 아니라 "성장 서사"를 강조하는 데 씁니다)
- 프로 작가 구간은 절제된 에디토리얼(editorial) 감성, 아마추어 작가 구간은 따뜻하고 접근하기 쉬운 무드로
  톤을 미세하게 분리합니다

### 컬러 팔레트 (확정: 듀얼 액센트 안)
| 역할 | Hex | 의미 |
|------|-----|------|
| Base | `#F5F1EA` (Warm Off-white) | 갤러리 화이트큐브 |
| Accent — 아마추어 구간 | `#C1602E` (Terracotta) | 붓·물감, 성장·따뜻함의 상징색 |
| Accent — 프로 구간 | `#1F2937` (Charcoal) | 절제된 전문성, 신뢰도 강조 |

프로/아마추어 구간을 액센트 색으로 직접 구분하여, 같은 페이지 내에서도 두 작가 유형의 톤 차이를 시각적으로 드러냅니다.

### 타이포그래피
- 국문 헤드라인: 세리프(serif) 계열 — 예술적 무게감
- 국문 본문: 고딕(sans-serif) 계열 — 가독성
- 영문 병기: Artist / Education / Artwork 등 핵심 용어는 자간을 넓힌 영문 라벨로 보조 표기해
  에디토리얼 갤러리 카탈로그 느낌을 냅니다

### 홈 화면 구조 (확정: 세로 스크롤 내러티브형)
작가 → 교육 → 작품 순서로 이어지는 하나의 스토리로 스크롤 노출합니다 (3분할 그리드형은 채택하지 않음).

### 핵심 페이지 구조
1. **홈** — 작가·교육·작품 3축을 한 화면 내러티브로 스크롤 노출 (히어로에 "잇다"는 메시지)
2. **작가 (Artists)** — 프로/아마추어 탭 분리, 각 프로필에 "작가 여정" 스토리 카드
3. **교육 (Education)** — 커리큘럼 목록, 코칭 신청, "전문 교육기관 진학 지원" 안내
4. **작품 (Artworks)** — 작품 사진·가격과 함께 작가의 배경 스토리를 연결해 노출 (단순 커머스 그리드 아님)
5. **전시·협회 (Exhibitions & Association)** — 협회 소개(향후 개설), 전시 일정, 홍보 콘텐츠
6. **문의/신청 (Contact)** — 작가 등록, 코칭 신청, 작품 문의 CTA

### 스토리텔링 원칙
- 작품 페이지에도 "이 작품을 만든 작가가 어떤 과정을 거쳤는지"를 함께 노출합니다
- 아마추어 작가 섹션은 커리큘럼 참여 전후의 성장 서사(Before/After)를 이미지·데이터로 제시합니다
- 프로 작가 섹션은 이력·전시 경력 등 신뢰도 데이터를 우선 노출합니다

## 기술 스택 (확정)

- **프레임워크**: Next.js (App Router)
- **스타일링**: Tailwind CSS
- **콘텐츠 저장소**: 별도 CMS 없이 `content/*.json`을 로컬 데이터 소스로 사용, 정적 생성(SSG)
- 향후 협회·교육 콘텐츠가 늘어나 비개발자의 직접 편집이 필요해지면 헤드리스 CMS(Sanity 등) 전환을 재검토합니다

## 인증 · 신청 심사 구조 (확정)

작가 등록/전환 신청은 로그인 기반으로 운영됩니다.

- **인증**: Auth.js(NextAuth v5) Credentials 로그인, JWT 세션에 `role`(`APPLICANT`/`ADMIN`) 포함
- **로그인 무차별 대입(brute force) 방어**: `User.failedLoginAttempts`/`lockedUntil` 필드로 관리
  (`src/lib/login-security.ts`, `src/auth.ts`) — 한 계정에서 비밀번호를 5회 연속 틀리면 15분간
  로그인 자체가 잠김(잠긴 동안은 정확한 비밀번호를 넣어도 거부). 로그인 성공 시 카운트 자동
  초기화. 잠금 상태는 `LoginForm`이 Auth.js의 `code=AccountLocked`로 구분해 일반 오류 메시지와
  다른 안내 문구를 보여줌.
- **데이터베이스**: SQLite + Prisma ORM (`@prisma/adapter-better-sqlite3` 드라이버 어댑터 필요 — Prisma 7부터 필수)
  - `User`(이름·전화번호·이메일·비밀번호 해시·비밀번호 힌트 질문/답변 해시·role), `Application`(type: PRO/AMATEUR, status: PENDING→RECEIVED→APPROVED/REJECTED), `Document`(첨부 증빙서류)
- **파일 업로드**: 증빙서류는 `public/`이 아닌 프로젝트 루트의 `uploads/<applicationId>/`에 비공개로 저장 (URL로 직접 노출되지 않음)
- **신청 흐름**:
  1. `/artists/pro`, `/artists/amateur`에서 폼 제출(성함·전화번호·이메일·비밀번호·비밀번호 힌트 질문/답변 + 학력/경력 또는 지원동기 + 파일) → 계정 자동 생성 + `Application` 생성(PENDING)
  2. 신청자는 `/login` 로그인 후 `/mypage`에서 본인 신청 상태 확인
  3. 운영자(role=ADMIN)는 `/admin`에서 전체 신청 목록을 보고 접수/승인/반려 처리 → 신청자 마이페이지에 즉시 반영
- **라우트 보호**: `src/middleware.ts`가 `/admin/*`은 ADMIN만, `/mypage/*`은 로그인 사용자만 접근하도록 차단
- **관리자 계정 시드**: `npm run db:seed` (`prisma/seed.ts`) — 기본 계정은 로그인 후 반드시 비밀번호를 변경할 것
- **비밀번호 변경/찾기**:
  - 로그인 후 `/mypage/settings`에서 비밀번호 변경 및 비밀번호 힌트(질문/답변) 등록·수정 가능 (모든 역할 공용, 이메일 자체는 변경 불가)
  - 로그인 없이 `/forgot-password`에서 이메일 입력 → 등록된 힌트 질문 확인 → 답변이 일치하면 새 비밀번호 설정 (답변은 `bcrypt` 해시로 저장·비교, 원문 노출 안 됨)
  - 힌트가 등록되어 있지 않은 계정(가입 시점 이전 계정 등)은 관리자가 `npm run reset-password -- <이메일> <새비밀번호>`(`scripts/reset-password.ts`)로 대신 처리
- 비개발자용 운영 절차(로그인·신청 심사·비밀번호 변경 등)는 [OPERATIONS.md](./OPERATIONS.md) 참고

## 승인 후 작가 서비스 (확정)

신청이 **승인(APPROVED)**되는 순간 `User.artistLevel`이 `PRO`/`AMATEUR`로 동기화되며(`/api/applications/[id]/status`),
마이페이지에 아래처럼 서로 다른 메뉴가 나타납니다.

- **프로 작가**: `/mypage/artworks/new`에서 작품 등록 — 제목·소개·**구분(동양화/서양화/조각/기타 중 라디오 버튼 선택,
  필수)**·**실제 가로×세로 크기(cm, 필수)**·**가격(직접 입력)**·**썸네일 이미지(선택)**를 함께 등록. 실제 크기는
  상세 페이지의 "ARTIEUM VIRTUAL" 가상 배치 미리보기 비율 계산에 쓰입니다(아래 참고). `ArtworkCategory`와 마찬가지로
  이 필드 추가 이전 등록작은 값이 비어 있을 수 있어, 작가가 `/mypage`의 "내 작품" 목록에서 직접 채워 넣을 수
  있습니다(`PATCH /api/artworks/[id]/size`, 본인 소유 작품만 가능). 등록 시 위탁판매 등록비(`LISTING_FEE`,
  `src/lib/pricing.ts`, 기본 3,000원)가 발생하며, 판매 완료 시 수수료 30%(`COMMISSION_RATE`)를 뗀 금액이 정산액으로
  계산됩니다. 이미지는 `public/artworks/`에 저장되며(비공개 증빙서류 `uploads/`와 달리 공개 노출용), 결제가
  `CONFIRMED`되어 `LISTED`(또는 `SOLD`) 상태가 되면 홈 화면 `#artworks` 섹션에 썸네일·작가명·가격과 함께 실제로
  노출됩니다 (`src/app/page.tsx`가 서버에서 직접 조회). 홈에는 최대 9개까지 슬라이드쇼로 보이는데,
  **단순 최신순이 아니라 "일별 순환 노출"**(`src/lib/home-rotation.ts`의 `getDailyFeaturedArtworks()`)
  방식입니다 — 작품이 몇 점이든 "최신 9개"만 계속 보여주면 등록 순서가 밀린 작가는 새 작품이 9개만
  더 등록돼도 홈에서 영영 사라지는 문제가 있어(2026-08-05, 사용자 요청으로 개선), 판매중/판매완료
  작품을 가진 작가 전원을 등록일(가장 처음 작품 기준) 오름차순으로 줄 세운 뒤 9명씩 **인터리브**
  (한 칸씩 건너뛰며) 묶어 하루에 한 묶음씩 노출합니다. 그래서 같은 날 묶음 안에도 오래된 작가와
  최근 작가가 고르게 섞이고, 전체 작가 수가 9명을 넘으면 며칠에 걸쳐 한 바퀴 돌며 전원이 노출됩니다
  (한국시간 자정 기준 자동 전환, DB 스키마 변경 없이 계산만으로 동작). 작가당 작품이 여러 점이면
  대표작도 날짜별로 돌아가며 바뀝니다. 등록 작품이 9개를 넘으면 **"전체 작품 보기" 링크**가 나타나
  `/artworks`(페이지당 12개, `?page=` 쿼리 기반 페이지네이션, 여긴 그대로 최신순)로 이동합니다.
  카드 UI는 `src/components/artwork-card.tsx`로 공용화되어 있습니다. `ArtworkCategory` enum 추가
  이전에 등록된 작품은 `UNCATEGORIZED`(미분류)로 마이그레이션되며, 작가가 `/mypage`의 "내 작품" 목록에서
  구분을 직접 수정할 수 있습니다 (`PATCH /api/artworks/[id]/category`, 본인 소유 작품만 가능).
- **아마추어 작가**: `/mypage/coaching/new`에서 코칭 예약 — ① 시/도 단위 간이 지도(`RegionMap`)에서 방문 가능
  지역 선택 → ② 해당 지역을 담당하는 강사 목록(학력·전시·수상경력 + 가능 요일 기반 다음 4주 날짜)에서 강사·날짜 선택
  (`InstructorPicker`) → ③ 배우고 싶은 내용(자유 텍스트) 입력 후 "신청 접수". 비용은 세션당 `COACHING_FEE`(기본 100,000원)로 동일.
  강사는 `/admin/instructors`에서 운영자가 이름·학력·전시·수상경력·담당 지역·가능 요일을 등록해야 목록에 노출됩니다.
- **결제 방식**: 온라인 결제 연동 없이 **계좌이체 + 운영자 수동 확인** 방식입니다.
  1. 작품/코칭 등록 시 `Payment` 레코드가 `WAITING` 상태로 함께 생성됩니다.
  2. 신청자가 마이페이지에서 입금자명을 입력해 신고합니다 (`/api/payments/[id]/declare`).
  3. 운영자가 `/admin/payments`에서 실제 입금 내역과 대조해 확인하면(`CONFIRMED`) 작품은 `LISTED`, 코칭 예약은 `CONFIRMED`로 자동 전환됩니다.
  4. 실제 작품이 판매되면 운영자가 `/admin/artworks`에서 "판매완료 처리"를 누르고, 이때 **최종 판매가**
     (`Artwork.finalPrice`, 기본값은 위탁 등록가 `price`로 미리 채워짐)를 확인·확정해 `SOLD`로 표시합니다.
     원칙적으로 위탁 등록가 그대로 판매되어야 하지만, 협상(네고)이 있었을 경우에만 값을 바꾸도록 안내하고
     다르면 화면에 경고 문구가 뜹니다. 수수료·작가 정산액은 이 `finalPrice`(없으면 `price`) 기준으로
     계산되며, `/admin/artworks`의 유통내역 추출(Excel/PDF/Word, `src/lib/artwork-export.ts`)도 이 값을
     "판매금액"으로 사용합니다 — 관공서 세무 제출용 매출장의 정확도를 위해 등록가가 아닌 실제 최종
     거래금액을 기준으로 삼습니다.
- **진품보증서 발급 (관리자 전용)**: `SOLD`로 표시된 작품에 한해 `/admin/artworks`에 "진품보증서 발급 (PDF)"
  버튼이 나타나며, 클릭 시 `/api/artworks/[id]/certificate`가 A4 세로 PDF를 즉시 생성해 내려줍니다
  (`src/lib/certificate.ts`가 작가명·작품명·재료·크기·에디션·판매일을 DB에서 그대로 가져와 매번 수기로
  작성할 필요가 없음, `src/lib/certificate-pdf.ts`가 pdfkit으로 렌더링). 작가명 (인) / 아트이음 대표
  윤진수 (인) 서명란이 포함되며, 대표자명·주소·연락처는 사이트 푸터(`site-footer.tsx`)와 동일한 값을
  씁니다. 작품 사진은 `sharp`로 리사이즈 후 JPEG로 재인코딩해 삽입합니다 — 원본 업로드가 수십MB PNG일
  수 있어(작품 등록 시 최대 50MB 허용) pdfkit에 원본을 그대로 넘기면 증서 PDF가 수십MB로 부풀려짐.
  일반 방문객·작가에게는 이 메뉴가 전혀 노출되지 않습니다(role=ADMIN만 접근 가능, 403).
- **계좌 정보**: `.env`의 `BANK_NAME`/`BANK_ACCOUNT_NUMBER`/`BANK_ACCOUNT_HOLDER` (플레이스홀더 상태 — 실제 계좌로 교체 필요, git에는 커밋되지 않음)
- **가격 컨설팅(무료)**: `/mypage/artworks/new`에서 `src/lib/price-suggestion.ts`의 `getPriceSuggestion()`이 계산한
  참고 가격대를 보여줍니다. 이 작가의 최근 판매완료(SOLD) 작품 최대 5건을 최신순 가중평균(1·0.8·0.6·0.4·0.2)한 뒤,
  승인된 프로 신청서의 전시 횟수(`Application.exhibitionCount`)만큼 보정(전시 1회당 +2%, 최대 +30%)하고,
  그 값의 -10%~+15%를 범위로 제시합니다. **판매 이력이 전혀 없으면 근거 없는 추측을 하지 않기 위해 추천값 없이
  안내 문구만 표시**합니다 — 데이터가 쌓이기 전까지는 신뢰도가 낮을 수 있음을 인지하고 있어야 합니다.
- **작가 프로필 노출 프리미엄 서비스 (프로 작가 전용)**: `/mypage/profile/new`에서 신청 — 증명사진(2x2, 정방형
  노출)·학력·전시경력·수상경력을 담은 `ArtistProfile`을 생성하고 `PROFILE_FEE` 결제(계좌이체+운영자 확인,
  기존 결제 흐름과 동일)가 `CONFIRMED`되는 순간 `isPublished=true`로 전환됩니다. 가격은
  `src/lib/pricing.ts`의 `getProfileFee()` — 정가 20,000원, 2026-11-30(KST)까지는 프로모션가 10,000원
  (신청 시점 날짜로 금액이 확정되는 스냅샷이라 운영자 확인이 늦어져도 소급 변경되지 않음). 신청 이후 내용
  수정(`PATCH /api/artist-profile`)은 추가 비용 없이 `/mypage`에서 언제든 가능하며, 결제 여부와 무관하게
  수정할 수 있습니다. 프로필당 1회만 신청 가능(이미 있으면 재신청 시 409). 이미지는 `public/profile-uploads/`에
  저장됩니다.

## 일반 방문객(컬렉터) 공개 노출 (확정)

작가가 아닌 일반 구매자("컬렉터")를 위한 공개 열람·가입 구조입니다.

- **역할 추가**: `Role`에 `COLLECTOR` 추가 — 작가 신청(승인 절차) 없이 `/signup`(`/api/signup`)에서
  이름·전화번호·이메일·비밀번호·비밀번호 힌트 질문/답변만으로 즉시 가입됩니다 (관리자 승인 불필요, 일반
  이커머스 회원가입과 동일).
- **공개 열람**: 홈 화면(`/`)과 전체 작품 목록(`/artworks`)은 **비회원도 열람 가능**합니다. 홈에는
  최신 9건이 자동 전환되는 슬라이드쇼(`ArtworkSlideshow`, 4.5초 간격, 이전/다음·인디케이터 포함)로 노출되고,
  하단 "전체 작품 보기"로 페이지네이션된 전체 목록(`/artworks`)에 연결됩니다. `/artworks`에는 검색바가 있어
  작품명·작가명(`q`), 구분(`category`, `src/lib/artwork-category.ts`의 `ArtworkCategory` 기준), 가격 범위
  (`minPrice`/`maxPrice`)로 필터링할 수 있으며 페이지네이션 이동 시에도 필터가 유지됩니다.
- **상세 페이지 게이팅**: `/artworks/[id]`는 `LISTED`/`SOLD` 상태만 접근 가능(그 외 404)하며,
  - 비로그인: 가격·설명·구매 방법·(있다면) PROFILE 버튼을 모두 숨기고 회원가입/로그인 유도 문구만 표시
  - 로그인(역할 무관, 컬렉터 포함 누구나): 가격·작품 설명 전체·"구매 문의하기"(mailto 기반, 온라인 결제 아님) +
    작가가 프로필 노출 서비스를 신청·결제해뒀다면(`artist.artistProfile?.isPublished`) 작가명 옆에 PROFILE
    버튼이 노출되어 클릭 시 사진·학력·전시경력·수상경력 팝업을 볼 수 있습니다(`ArtistProfileButton`).
    **의도적으로 회원 전용**입니다 — 비회원도 볼 수 있는 "ARTIEUM VIRTUAL"과 달리, 작가 프로필은 "구매를
    고민하는 컬렉터가 신뢰 정보를 얻으려면 가입해야 한다"는 가입 유인으로 쓰기 위함입니다.
- 카드(`ArtworkCard`)·슬라이드쇼 썸네일 모두 `/artworks/[id]`로 링크됩니다.
- **찜하기**: 로그인한 사용자(역할 무관)는 `Favorite`(userId+artworkId 유니크) 레코드로 작품을 찜해둘 수
  있습니다. 하트 아이콘(`FavoriteButton`)이 `/artworks` 카드 이미지 우상단(오버레이, 클릭해도 상세
  페이지로 이동하지 않음)과 `/artworks/[id]` 가격 옆(인라인, 로그인 회원에게만 — PROFILE 버튼과 같은
  원칙) 두 곳에 있습니다. 홈 화면 슬라이드쇼에는 아직 없습니다(범위 밖). `/mypage`의 "찜한 작품"
  섹션에서 다시 모아볼 수 있으며, 판매완료(SOLD)된 작품도 목록에서 지워지지 않고 배지만 그대로
  보입니다. 작품이 삭제되면(테스트 데이터 정리 스크립트 등) `Favorite`도 `onDelete: Cascade`로 함께
  정리됩니다.
- **"ARTIEUM VIRTUAL" 가상 배치 미리보기**: 작품에 실제 크기(`widthCm`/`heightCm`)와 썸네일이 모두 등록돼 있으면,
  상세 페이지(`/artworks/[id]`) 하단에 로그인 여부와 무관하게 노출됩니다(등록비 3,000원 대비 차별화 포인트로,
  가입 유도가 아니라 누구나 볼 수 있게 함). 실사 사진 대신 Artlink 디자인 컨셉(붓·물감을 연상시키는 웜톤
  강조색 + 화이트큐브 에디토리얼 톤)에 맞춘 현관·거실·사무실 3개의 SVG 플랫 일러스트 공간
  (`src/components/artieum-virtual-preview.tsx`)에 작품 이미지를 실제 크기 비율 그대로 배치해 보여줍니다.
  각 공간은 벽/바닥 색과 가구(현관: 문·콘솔 테이블·화분, 거실: 소파·러그·창·플로어 램프, 사무실: 책상·모니터·
  의자·블라인드)로 분위기를 구분하되, 세 공간 모두 화분을 공통으로 배치해 "성장 서사" 톤을 은근히 반복합니다.
  세 공간 모두 SVG 좌표 1단위 = 실제 1cm로 동일한 축척을 써서, 작품의 `widthCm`/`heightCm` 값을 그대로 도형
  크기로 사용하는 방식으로 비율을 맞춥니다(문 200cm·소파 등받이 80cm·책상 75cm 등 기준 가구 높이로 크기 감을
  줌). 제목은 테라코타 색상으로 "ARTIEUM VIRTUAL"로 표기합니다.

## 폴더 구조

| 폴더 | 용도 |
|------|------|
| `src/app/` | Next.js App Router 페이지·레이아웃 (`login/`, `mypage/`, `admin/`, `artists/pro`, `artists/amateur`, `api/`) |
| `src/components/` | 공용 UI 컴포넌트 (SiteHeader, ApplicationForm, AdminApplicationRow 등) |
| `src/auth.ts` / `src/auth.config.ts` / `src/middleware.ts` | Auth.js 인증 설정 및 라우트 보호 |
| `src/lib/prisma.ts` | Prisma 클라이언트 싱글턴 |
| `src/lib/pricing.ts` | 위탁판매 등록비·코칭 비용·수수료율·계좌 정보 config |
| `src/lib/regions.ts` | 시/도 지역 목록 및 간이 지도 그리드 배치 |
| `src/lib/schedule.ts` | 요일 코드/라벨, 강사 가능 요일 기반 다음 날짜 계산 |
| `prisma/` | DB 스키마(`schema.prisma`)·마이그레이션·시드 스크립트 |
| `uploads/` | 신청자 증빙서류 원본 파일 (비공개, git 추적 제외) |
| `public/` | 정적 파일(이미지 등) |
| `content/` | 전시·작품 콘텐츠 데이터 (`exhibitions.json`, `artworks.json`) |
| `.claude/commands/` | 슬래시 커맨드(Skill) 정의 — 반복 작업 패턴을 `.md` 파일로 등록 |
| `.claude/hooks/` | Write/Edit 등 이벤트에 반응하는 자동 검증 스크립트 |
| `.claude/agents/` | 특정 역할에 특화된 서브에이전트 정의 |

## 주요 작업 패턴 (Skills)

| Skill | 용도 | 핵심 흐름 |
|-------|------|----------|
| `/update-exhibition` | 전시·작품 콘텐츠 등록·수정 | 대상 식별 → 기존 데이터 확인 → 필드 검증(날짜·status·가격·exhibition_id 교차검증) → JSON 반영 → 결과 보고 |

## Hooks (자동 검증)

_아직 등록된 Hook이 없습니다. `.claude/hooks/`에 스크립트를 추가하고 `.claude/settings.json`에 연결한 뒤 이 표에 기록하십시오._

| Hook | 트리거 | 검증 내용 |
|------|--------|----------|

## MCP

| 서버 | 용도 |
|------|------|
| `artlink-filesystem` | 프로젝트 폴더 내 파일 목록 조회·읽기 (`.claude/artlink_mcp.py`) |
