# Artlink 운영 가이드

개발자가 아니어도 이 문서만 보고 사이트를 실행하고, 작가 등록·전환 신청을 접수·심사할 수 있도록 정리했습니다.

> ⚠️ **이 문서에는 초기 관리자 비밀번호가 그대로 적혀 있습니다.** 2번 항목의 안내대로
> 비밀번호를 변경하기 전까지는 이 파일을 외부에 공유하거나 깃(git) 등에 올리지 마세요.
> 비밀번호를 바꾼 뒤에는 아래 값이 더 이상 유효하지 않으니 안심하고 보관해도 됩니다.

## 1. 사이트 실행하기

1. 터미널(PowerShell)에서 `Artlink` 폴더로 이동합니다.
2. 아래 명령을 실행합니다.
   ```
   npm run dev
   ```
3. 실행 후 나오는 `Local: http://localhost:3000` 주소를 브라우저에 입력해 접속합니다.
4. 종료하려면 터미널 창에서 `Ctrl + C`를 누릅니다.

> 서버를 켜둔 상태에서만 사이트 접속과 관리자 화면 사용이 가능합니다. (아직 외부 서버에 올라가 있지 않고, 내 컴퓨터에서만 실행되는 상태입니다.)

## 2. 관리자로 로그인하기

1. 브라우저에서 `http://localhost:3000/login` 접속
2. 아래 기본 관리자 계정으로 로그인합니다.
   - 이메일: `admin@artlink.local`
   - 비밀번호: `artlink-admin-2026!`
3. 로그인 후 우측 상단 메뉴의 **관리자** 링크를 누르면 신청 접수 관리 화면(`/admin`)으로 이동합니다.

**비밀번호를 반드시 바꾸세요.** 아직 화면에서 직접 바꾸는 기능은 없어서, 터미널에서 아래 명령으로 변경합니다.
```
npm run reset-password -- admin@artlink.local 새비밀번호(8자이상)
```

## 3. 신청 접수·심사 처리하기

1. `/admin` 화면에 접속하면 접수된 신청이 최신순으로 나열됩니다.
2. 각 신청 카드의 **"상세 보기"**를 누르면 전화번호·이메일·학력·경력(또는 지원 동기)·첨부 서류를 확인할 수 있습니다.
3. 첨부 서류는 파일명을 클릭하면 바로 열람·다운로드됩니다.
4. 내용을 확인했다면 아래 버튼 중 하나를 눌러 상태를 변경합니다.
   - **접수 처리**: 서류를 확인했다는 표시 (아직 최종 결정 전)
   - **승인**: 프로/아마추어 작가로 최종 승인
   - **반려**: 신청을 거절
5. 상태를 바꾸면 신청자가 본인 마이페이지(`/mypage`)에서 바로 확인할 수 있습니다. 별도로 연락하지 않아도 되지만, 반려한 경우에는 이메일이나 전화로 사유를 안내하는 것을 권장합니다.

## 4. 신청자는 어떻게 이용하나요

1. 홈페이지 "작가" 섹션에서 **프로 작가** 또는 **아마추어 작가** 카드를 눌러 신청 페이지로 이동합니다.
2. 성함·전화번호·이메일·비밀번호와 필요한 정보(학력·경력 또는 지원 동기), 증빙서류를 첨부해 제출합니다.
   - 이때 입력한 이메일·비밀번호로 계정이 자동으로 만들어집니다.
3. 제출 후 `/login`에서 로그인하면 `/mypage`에서 본인 신청 상태(제출됨 → 접수 → 승인/반려)를 확인할 수 있습니다.

## 5. 승인 후 서비스 이용 안내 (위탁판매·코칭)

작가 신청이 **승인**되면 신청자 마이페이지에 새 메뉴가 나타납니다.

- **프로 작가**: "내 작품 등록" — 작가가 직접 가격을 입력해 작품을 등록합니다. 등록 시
  위탁판매 등록비(기본 50,000원)가 발생하고, 실제 판매되면 수수료 30%를 뗀 금액이 정산액입니다.
- **아마추어 작가**: "코칭 예약" — ① 지도에서 방문 가능 지역 선택 → ② 그 지역을 담당하는 강사 목록(학력·전시·수상경력과
  가능 요일 기준 다음 4주 날짜)에서 강사와 날짜 선택 → ③ 배우고 싶은 내용 입력 → "신청 접수". 세션당 비용(기본 100,000원)이 발생합니다.
  **강사가 미리 등록되어 있어야 목록에 나타납니다** — 3-1번 항목 참고.

### 4-1. 강사 등록하기 (코칭 예약에 필요)

아마추어 작가가 코칭을 예약하려면 해당 지역을 담당하는 강사가 먼저 등록되어 있어야 합니다.

1. 관리자 화면(`/admin`) 상단의 **"강사 관리"** 링크(`/admin/instructors`)로 이동합니다.
2. 하단 "새 강사 등록" 폼에 강사명·학력·전시 이력·수상 경력을 입력합니다.
3. **담당 지역**(예: 서울특별시, 경기도 등 복수 선택 가능)과 **가능 요일**(예: 화·목)을 버튼으로 선택합니다.
4. "강사 등록"을 누르면 즉시 목록에 반영되고, 아마추어 작가가 해당 지역을 선택했을 때 노출됩니다.

**입금 확인 절차** (온라인 결제가 아니라 계좌이체 방식입니다):
1. 작가가 작품 등록/코칭 예약을 하면 화면에 입금 계좌 정보(은행명·계좌번호·예금주)와 금액이 안내됩니다.
2. 작가가 실제로 입금한 뒤, 화면에서 입금자명을 입력해 "입금 완료 신고"를 누릅니다.
3. 운영자는 `/admin/payments`(관리자 화면 상단 **"입금 확인"** 링크)에서 신고된 입금자명과 실제 통장 내역을 대조합니다.
4. 일치하면 **"입금 확인"** 버튼을 누릅니다 — 작품은 자동으로 "위탁판매 중"으로, 코칭은 "예약 확정"으로 바뀌고 작가에게 즉시 반영됩니다.
5. 실제로 작품이 팔리면 `/admin/artworks`(관리자 화면 상단 **"작품 관리"** 링크)에서 해당 작품의 **"판매완료 처리"**를 누릅니다. 수수료 30%와 작가 정산액이 화면에 함께 표시됩니다 — 실제 계좌 정산(작가에게 돈을 보내는 것)은 운영자가 직접 처리해야 합니다.

> ⚠️ **계좌 정보를 반드시 설정하세요.** `.env` 파일의 `BANK_NAME`, `BANK_ACCOUNT_NUMBER`, `BANK_ACCOUNT_HOLDER`
> 값이 아직 예시(플레이스홀더)로 되어 있습니다. 실제 계좌 정보로 바꾸지 않으면 작가 화면에 잘못된 계좌가 안내됩니다.

## 6. 자주 발생할 수 있는 상황

| 상황 | 확인할 것 |
|------|----------|
| 사이트가 안 열림 | `npm run dev`를 실행한 터미널 창이 켜져 있는지 확인 |
| 로그인이 안 됨 | 이메일·비밀번호 오타 확인. 관리자 비밀번호를 잊었다면 `npm run reset-password`로 재설정 |
| 새 신청이 안 보임 | `/admin` 화면을 새로고침 |
| 첨부 파일이 안 열림 | 파일이 저장된 `uploads/` 폴더가 삭제·이동되지 않았는지 확인 |

## 7. 아직 없는 기능 (참고)

- 관리자·신청자가 화면에서 직접 비밀번호를 바꾸는 기능 (현재는 터미널 명령으로만 가능)
- 반려 사유를 시스템에서 자동으로 신청자에게 이메일 발송하는 기능
- 실제 외부 서버 배포(현재는 내 컴퓨터에서 `npm run dev`로 실행해야만 접속 가능)
- 온라인 결제 자동화(카드 결제 등) — 현재는 계좌이체 + 운영자 수동 확인 방식만 지원
- 판매 대금을 작가 계좌로 자동 정산하는 기능 — 정산 금액 계산까지만 자동이며, 실제 송금은 운영자가 직접 처리

이 문서는 운영 방법이 바뀌거나 새 기능이 추가될 때 함께 업데이트해야 합니다.
